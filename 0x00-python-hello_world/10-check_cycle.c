#include "lists.h"
/**
 * check_cycle - checks if a linked list as a loopy cycle
 * @list: pointer to the head of node
 * Return: o if no cycle and 1 if otherwise
 */

int check_cycle(listint_t *list){
    listint_t *p2
}